{
  "buildCommand": "pnpm build",
  "devCommand": "pnpm dev",
  "installCommand": "git config --global url.\"https://$GITHUB_TOKEN@github.com/\".insteadOf \"https://github.com/\" && pnpm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "git": {
    "deploymentEnabled": {
      "main": true,
      "staging": true
    }
  }
}
