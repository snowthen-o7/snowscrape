{
  "buildCommand": "pnpm build",
  "devCommand": "pnpm dev",
  "installCommand": "node scripts/setup-git-auth.cjs && pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "regions": ["iad1"],
  "git": {
    "deploymentEnabled": {
      "main": true,
      "staging": true
    }
  }
}
