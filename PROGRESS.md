# SnowScrape -- Progress & Launch Readiness

**Last Updated:** 2026-06-15
**Launch Readiness:** ~98% (the core scraping product is live; the Google Docs destination is a SECONDARY, currently NON-FUNCTIONAL export path, not part of the live surface, see the deferral below. OAuth approach REFINED 2026-06-12: reuse the existing SnowForge Google Cloud **project** but mint a **NEW dedicated OAuth client ID** for SnowScrape (own `client_id`/`client_secret` in Doppler `sf-snowscrape`, isolated from SnowPipe's Merchant Center client, so no shared-secret juggling; keeps the project's already-enabled APIs + consent screen, so no new-project verification cycle). DONE (Alex, 2026-06-12): Drive + Docs APIs enabled; new client created; `client_id`/`client_secret` in Doppler; redirect URIs registered on the new client; `drive.file` scope added; `GOOGLE_OAUTH_REDIRECT_URI` set in Doppler dev/prod. **DEFERRED: the `documents` scope** (sensitive; on this shared project, whose consent screen is likely Published for SnowPipe's live Merchant Center, adding it would trigger a project-wide re-verification that touches SnowPipe). Google Doc support is not critical for SnowScrape right now. **CONSEQUENCE: the Docs-export feature is non-functional until `documents` is added** -- `_create_doc` creates an empty Doc via Drive (`drive.file`) then writes ALL content via `docs.documents().batchUpdate` (`documents`), which 403s without that scope. **BACKEND DEPLOY ON HOLD** (no value provisioning the KMS key / 3 DynamoDB tables / SQS queue+DLQ / Lambda for a path that can't write content; nothing is user-facing yet since the frontend is still on unmerged `claude-main`). To resume: add `documents` (or stand up a separate Testing-mode GCP project to grant it with no verification + no SnowPipe impact, or re-scope to drive.file-only via Drive upload-convert), then `sst deploy` dev+prod + smoke-test.)
**Build Status:** PASSES (backend unit suite 849 passed / 3 skipped green as of 2026-06-15, +34 scraper_preview characterization tests this cycle, +56 pdf_handler the prior cycle; integration unchanged/not re-run this cycle; frontend lint 0 errors + vitest 121/121 + prod build green on claude-main as of 2026-06-11)
**Test Coverage:** ~60-70% (unit + integration; Playwright setup present)

### Recent -- 2026-06-15
- Characterization tests for `scraper_preview.py` (claude-main, no source change, closes #41 on merge to main). `scraper_preview` backs the no-code Visual Scraper Builder: after a page is fetched via the tiered scraper, three pure helpers turn each BeautifulSoup element into the XPath / CSS selector / DOM-path crumb the UI shows, and `test_extraction` runs a user's selector set against a freshly scraped page so they can verify a config before saving. None of it had dedicated coverage, yet a silent regression here is a correctness bug the user only discovers after a job runs and extracts nothing (a generator emitting a selector that does not match the element it describes, or `test_extraction` mis-dispatching a selector type / swallowing a match). Added `backend/tests/unit/test_scraper_preview.py` (34 tests, no source change). The pure generators run against real BeautifulSoup tags; `test_extraction` calls the async `smart_scrape` through its own private event loop, so the suite monkeypatches `scraper_preview.smart_scrape` (the name the module bound at import) with an async fake returning a controllable `(result, tier_used, escalation_log)` tuple while the lxml/BeautifulSoup extraction runs for real. Pins: `generate_css_selector` (id wins over class, first-class-only, bare tag, tag-name preserved); `generate_dom_path` (id-vs-class per node, document-root excluded, and the 6-nearest-nodes depth cap with the top ancestors truncated, matching the `insert(0,...)+break` walk); `generate_xpath` including the two deliberately-pinned quirks (it ignores its `tree` argument entirely and only the FIRST class reaches the `contains(@class, ...)` predicate); and `test_extraction` dispatch (xpath element `text_content` vs `//title/text()` `str()` branch, css `get_text`, regex group(1)-vs-group(0)-vs-None, unknown-type -> None, invalid-XPath isolated per-selector to None, the no-`soup`-key content fallback, tier-1/tier-2 `_tier_info` pass-through, the friendly "Failed to fetch page" wrap when `smart_scrape` raises, and the latent `TIER_INFO[tier_used]` KeyError on an unexpected tier). Independent diff-only adversarial review (fresh code-reviewer subagent, saw test + source + tiered_scraper TIER_INFO): APPROVE WITH NITS, no HIGH/MEDIUM false-greens; it verified each non-obvious claim by direct execution (the tree-ignored / first-class quirks, the 6-node truncation order, the `text()` str-branch, the invalid-xpath isolation, the no-soup fallback). Folded its one actionable LOW before merge (added the `TIER_INFO[tier_used]` KeyError pin); dismissed the rest as out-of-scope (`fetch_and_parse_page` not in #41) or inherent to characterization (escalation-log pass-through, lxml string-result internals). No web build (Python backend; tests are the gate). Evidence: new file 34 passed; full backend unit suite 849 passed / 3 skipped / 0 failed (was 815; +34). Squash-merged into LOCAL claude-main; LOCAL ONLY (push-free loop; Alex pushes claude-main + opens the claude-main -> main PR himself).
- Characterization tests for `pdf_handler.py` (claude-main, no source change). `pdf_handler` is the scrape engine's only PDF-to-structured-data path: when a crawled URL serves a PDF, `crawl_manager` dispatches every `pdf_*` query here (crawl_manager's own tests treat this handler as an isolated black box), yet it had ZERO dedicated coverage. A silent regression here is a correctness/data-loss bug: a query type quietly returning None, the pipe-join contract changing, the table flatten/column-filter logic drifting, or one bad query aborting the batch. Added `backend/tests/unit/test_pdf_handler.py` (56 tests, no source change). `pdfplumber` is a real backend dep, but building PDFs with known text/tables is brittle, so `pdfplumber.open` is faked to return controllable pages (effective because the source's function-local `import pdfplumber` resolves the same cached module object the test patches); the `process_pdf_query` dispatch layer is isolated by patching the three module-level `extract_pdf_*` helpers it calls by bare name. Pins: `is_pdf_content` content-type substring/case-insensitivity + `%PDF-` magic bytes + the `len >= 5` boundary (`b"%PDF"` is False) + content-type-wins-before-magic; `extract_pdf_text` page-range clamp math (two-element inclusive end, single-element one-page, negative-start clamp to 0, end clamp to total), join-to-string vs list, None page text -> ""; `extract_pdf_tables` cell cleaning (None -> "", non-str -> str, strip), header-keying with the `column_{i}` fallback for both over-long rows and blank header cells, newline-in-header replacement, empty-table skip, multi-table table_index, page-range, and table_settings pass-through; `extract_pdf_metadata` field mapping/defaults/page_count; `process_pdf_query` dispatch (text verbatim vs regex `re.findall` + the `if join_flag and results` empty-stays-`[]` contract, table index-select / single-vs-list fallback / flatten single+multi / named-column filter + join / column-filter-skipped-when-list, metadata pass-through ignoring query/join/config, unknown type -> None, and an extractor exception isolated to None); and `process_pdf_queries` batch dispatch (non-`pdf_` and missing-type queries skipped to None WITHOUT reaching `process_pdf_query`, `unnamed` name default, multi-query independence, empty list -> {}). Independent diff-only adversarial review (fresh code-reviewer subagent, saw source + test): APPROVE, no HIGH/MEDIUM false-greens; it traced every page-range/fallback/empty-join/isinstance branch against the source, confirmed all monkeypatch targets resolve the references the source uses at call time and that the `pdfplumber.open` fake is effective for the function-local import style; its one cosmetic LOW (the metadata pass-through test did not prove query/join/pdf_config are ignored) was folded in before merge. No web build (Python backend; tests are the gate). Evidence: new file 56 passed; full backend unit suite 815 passed / 3 skipped / 0 failed (was 759; +56). Squash-merged into LOCAL claude-main; LOCAL ONLY (push-free loop; Alex pushes claude-main + opens the claude-main -> main PR himself).

### Recent -- 2026-06-14
- Characterization tests for `websocket_handler.py` + a subscribe-path bug fix (claude-main, closes #39 on merge to main). The real-time WebSocket layer (the `$connect`/`$disconnect`/`$default` Lambda routes plus the outbound fan-out) had ZERO dedicated tests despite being both a SECURITY boundary and a reliability boundary. The security surface: `ws_default_handler` must refuse every action except `authenticate` until `is_connection_authenticated` is true, otherwise an unauthenticated socket could `subscribe` to and receive another user's job stream; the pending (unauthenticated) connection also carries a deliberately SHORT 300s TTL that only extends to 24h after auth. The reliability surface: a regression that drops a subscribed connection leaves a client blind to job completion, and a regression in the GoneException cleanup leaves stale connections piling up broadcast errors. Added `backend/tests/unit/test_websocket_handler.py` (48 tests; reloads the module inside moto via a `ws` fixture that creates the `SnowscrapeConnections-test` table the conftest does not, since the module caches `CONNECTIONS_TABLE`/`connections_table` at import; the apigatewaymanagementapi client is faked because moto's support is thin and the source only calls `post_to_connection` + catches `client.exceptions.GoneException`, and `validate_clerk_token` is patched per test) pinning: `ws_connect_handler` stores a pending row (`user_id=__unauthenticated__`, `authenticated=False`, empty `subscriptions`) with `ttl == connected_at + 300`, missing connectionId -> 400, put failure -> 500; `ws_disconnect_handler` deletes the row, missing id -> 400, delete failure -> 500; `is_connection_authenticated` true/false/no-item/absent-attribute-defaults-false/ddb-error-false; `ws_default_handler` routing (missing id -> 400, invalid JSON -> 400, `authenticate` action dispatched even while unauthenticated, the auth GATE rejecting a `subscribe` from an unauthenticated socket BEFORE `handle_subscribe` is reached, authenticated dispatch to handle_auth/subscribe/unsubscribe, `ping` -> `pong`, unknown type -> error, unexpected exception -> 500); `handle_authenticate` (no token -> auth_error, valid token upgrades the row to `authenticated=True`+user_id and extends ttl to ~+86400 returning auth_success, token without `sub` -> auth_error and row NOT upgraded, validate raising -> auth_error with the exception message); `handle_auth` token-refresh (no token, valid updates user+ttl, invalid -> auth_error); `handle_subscribe`/`handle_unsubscribe` (no channel -> 400, ADD/DELETE on the DynamoDB string set asserted against moto incl. multi-channel accumulation and removing only the named channel, ddb error -> 500); `send_to_connection` (posts the payload + 200, GoneException reaps the row + 410, generic error -> 500); `broadcast_to_channel`/`broadcast_to_user` (sends to all matching subscribers/user connections via the `contains(subscriptions, :channel)` / `user_id = :uid` scan, honors `exclude_connection`, reaps stale GoneException connections while keeping healthy ones, swallows a top-level scan error without raising); and `notify_job_status_change` (no `WS_API_DOMAIN` is a silent no-op; with it set, broadcasts to `job:{id}` then `jobs:all` then the user with the `{type: job:status, data: {job_id, status, **job_data}}` message shape on the configured domain/stage). FOUND + FIXED a real prod reliability bug while characterizing the subscribe path (the independent diff-only review surfaced it): `ws_connect_handler` seeded `subscriptions: []` (an empty DynamoDB LIST), but `handle_subscribe` extends it with `ADD subscriptions :channel` (a string SET), and DynamoDB rejects `ADD` against a list operand with a ValidationException, so the FIRST subscribe after a real `$connect` returned 500 ("Subscription error") and the client silently received no live job updates for that channel. TDD: added an end-to-end `connect -> subscribe` test that reproduced the 500, then fixed the source by NOT seeding `subscriptions` at connect (leaving the attribute absent lets `ADD` create the set; `contains(subscriptions, ...)` in the broadcast scan already treats an absent attribute as "subscribed to nothing", so no read path regresses). After the fix the lifecycle test passes 200 and the channel set contains the subscription. The 48 characterization tests above pin the rest of the module unchanged. No web build (Python backend; tests are the gate). Evidence: new file 49 passed (48 characterization + 1 lifecycle regression guard); full backend unit suite 759 passed / 3 skipped / 0 failed (was 710; +49). Squash-merged into LOCAL claude-main; LOCAL ONLY (push-free loop; Alex pushes claude-main + opens the claude-main -> main PR himself).
- Characterization tests for `webhook_dispatcher.py` (claude-main, no source change, closes #37 on merge to main). `WebhookDispatcher.dispatch_event` is the INBOUND half of the webhook system (the delivery handler, #36, is the outbound half): on every job state change it queries the user's active webhooks (GSI `UserIdIndex`, `FilterExpression active = :active`), keeps only the ones whose `events` list contains the fired event type, and enqueues ONE SQS message per match in the exact shape the delivery Lambda consumes. It had ZERO dedicated tests despite being a reliability/contract boundary: a regression here either drops a subscribed webhook (the user goes blind to job completion), fires an inactive/foreign webhook (notification leak), or drifts a message field name (`webhook_url`/`webhook_secret`/`payload`) so every downstream delivery breaks. Added `backend/tests/unit/test_webhook_dispatcher.py` (25 tests, reload-in-moto like the delivery-handler suite; `sqs_client.send_message` stubbed with a Mock so the message body + per-send failure handling are asserted without a real queue) pinning: match/count logic (single match -> 1 + one send; no webhooks -> 0 + no send; not-subscribed/empty-events -> filtered; inactive excluded at the DDB FilterExpression before the event filter; only-subscribed-of-many counted; another user's webhook never selected via the GSI key) and the characterization that the first match clause `event_type in events` IGNORES `job_id`, so a webhook carrying a foreign `job_id` but subscribed to the event STILL fires (job_id is not an effective scope); the SQS message shape matching the delivery handler contract (`webhook_id`/`webhook_url` from `url`/`webhook_secret` from `secret`/`event_type`/`job_id`/`user_id`/`payload` + a non-empty `delivery_id`), missing secret serialized as `''`, the QueueUrl is the configured `webhook_queue_url`, a distinct uuid `delivery_id` per webhook, the standard-queue branch passing `MessageGroupId`/`MessageDeduplicationId` as None vs the `.fifo` branch setting group=webhook_id + dedup=delivery_id; failure isolation (one send raising is caught per-iteration so others still count; all raising -> 0; the webhooks query raising is swallowed by the outer try/except -> 0 and no send); and every convenience wrapper (`dispatch_job_created/started/completed/failed/cancelled`) building its documented payload (correct event name, `job_name` from `name` defaulting to None, `link_count` defaulting to 0, started/completed/failed/cancelled timestamps all read from `last_run`, completed carries `results_s3_key` + `summary`, failed carries `error`) and returning `dispatch_event`'s count verbatim. No source change (regression hardening). No web build (Python backend; tests are the gate). Evidence: new file 25 passed; full backend unit suite 710 passed / 3 skipped / 0 failed (was 685; +25). Squash-merged into LOCAL claude-main; LOCAL ONLY (push-free loop; Alex pushes claude-main + opens the claude-main -> main PR himself). Also reconciled the PROGRESS headline drift the 2026-06-13 daily report (d) flagged: the "~98% / Google Docs backend complete" framing overstated shippability while the Docs export is non-functional (the sensitive `documents` scope is deferred); the Launch Readiness line now states up front that Google Docs is a secondary, currently non-functional export path, not part of the live surface.
- Characterization tests for `tiered_scraper.py` (claude-main, no source change, advances #38 for the core-scrape-engine modules; with proxy_manager + crawl_manager already done, this closes the last named module in #38). `tiered_scraper` is the live scrape engine: every crawled URL flows through `smart_scrape`, which starts at the cheapest tier and escalates on blocking. The Firecrawl tiers (3/4) already had coverage in `test_firecrawl_tiers.py`, but the cheap default path, the proxy path, the blocking heuristic, and the escalation state machine had ZERO dedicated tests, despite being correctness/abuse/cost boundaries (a false-negative block scrapes a challenge page as real data; a false-positive needlessly escalates to a 50x-pricier tier; a proxy-resolution bug routes outbound traffic wrong). Added `backend/tests/unit/test_tiered_scraper.py` (44 deterministic tests, real `FakeResponse` not MagicMock so `text.lower()` in the detector behaves) pinning: `detect_blocking` -- the 403/429/503 status whitelist (and that 404/500 alone are NOT indicators), every content challenge pattern (cloudflare/access-denied/human-verification/datadome/`net::err_`/neterror) matched case-insensitively via `text.lower()`, the `content`-arg-overrides-`response.text` branch both directions, and the `minimal_content` rule (<200 stripped chars, gated on an existing indicator OR status>=400, so a tiny clean 200 page is NOT flagged) plus indicator accumulation; `scrape_tier_1` success metadata (tier/name/cost 0.0001, soup, forwarded UA header + timeout + `allow_redirects`), the SSRF pre-check raising `URLRequired` before any network call, and blocking raising `BlockingDetectionError(tier_used=1)`; `scrape_tier_2` proxy resolution order (provided config https-before-http > `RESIDENTIAL_PROXY_URL` env > AWS pool via `get_proxy_manager` > `NotImplementedError` when none, incl. the `except Exception` swallow that keeps `proxy_url` None), the `verify=False` proxy-SSL contract, the geo `any`/random/no-direct-fallback config the tier requests, and tier-2 blocking; `smart_scrape` escalation (return first success; escalate on `BlockingDetectionError`; do NOT escalate on `NotImplementedError`, surfacing the tier-specific "Week 1" guidance and proving the next tier is never called; escalate on generic Exception; the `max_tier` cap; `auto_escalate=False`; and `min_tier` start proving cheaper tiers are skipped) driven via `asyncio.run` (the suite uses no asyncio marker, honoring `--strict-markers`); and the pure `get_tier_info`/`estimate_cost`/`TIER_INFO[1]/[2]` cost helpers. Independent diff-only adversarial review (fresh code-reviewer subagent, saw test + source + proxy_manager + validators): APPROVE, no HIGH/MEDIUM false-greens; it confirmed all patch targets resolve the references the source actually uses at call time (incl. that patching `proxy_manager.get_proxy_manager` is correct for the call-time `from proxy_manager import` rebind, and that monkeypatching `tiered_scraper.scrape_tier_N` replaces what `smart_scrape`'s call-time `tier_functions` dict dispatches), and that the NotImplemented/min_tier tests use positive not-called probes so they cannot pass for the wrong reason. Its two MEDIUMs (the AWS-pool config was asserted INSIDE `get_proxy_url`, where the source's `except Exception` would swallow a regression into a misleading NotImplementedError; and the geo/rotation fields were claimed but unasserted) were folded in before merge by capturing the config to an outer dict and asserting `enabled`/`geo_targeting`/`rotation_strategy`/`fallback_to_direct` after the call; its two LOWs (assert both proxy slots on the http-fallback; assert `allow_redirects` on the live request path) were also folded in. No source change (regression hardening). No web build (Python backend; tests are the gate). Evidence: new file 44 passed; full backend unit suite 685 passed / 3 skipped / 0 failed (was 641; +44). Squash-merged into LOCAL claude-main; claude-main now 37 commits ahead of origin/main, LOCAL ONLY (push-free loop; Alex pushes claude-main + opens the claude-main -> main PR himself).

### Recent -- 2026-06-13
- Characterization tests for `crawl_manager.py` (claude-main, no source change, advances #38 for the core-scrape-engine modules). `crawl_manager.process_queries` is the live extraction chokepoint: every scraped page (HTML/JSON/PDF + Firecrawl markdown) is dispatched here to the right extractor (xpath/regex/jsonpath/pdf_*/ai), the optional pipe-join is applied, and a failing query is isolated so one bad selector never sinks the whole crawl, yet it had ZERO dedicated coverage. A silent regression here is correctness/data-loss: a query type quietly returning None, the join contract changing, or one query's exception aborting the batch. Added `backend/tests/unit/test_crawl_manager.py` (29 tests + 3 SIGALRM-skipped on Windows, no source change) pinning: xpath text-node `str()` coercion / multi-result lists / pipe-join / empty-match `[]` (incl. the `if join_flag and results:` empty-stays-`[]` contract) / malformed-XPath isolated as None / `unnamed` name default; jsonpath scalar+array+join and JSON-parse-failure-on-HTML to None; the regex dispatch + join + RegexTimeoutError to None (with `safe_regex_findall` isolated behind monkeypatch so the path is pinned cross-platform without depending on the Unix-only SIGALRM runner); the PDF query types (handler isolated) incl. the no-expression exemption for `pdf_*`, handler-owns-its-join (return value verbatim), pdf-query-on-non-PDF to None, and xpath-on-PDF to None (HTML tree never built); the AI branch (`extract_with_ai` patched on the lazily-imported `ai_extractor` module) preferring markdown over decoded HTML, the html fallback, and failure isolated to None; plus dispatch contracts (empty list -> {}, unknown type -> None, missing expression for an HTML type skipped entirely so the name is absent, multi-query independence); and `get_crawl` against moto (found-item field mapping, `state`/`attempts`/`results` defaults, not-found -> None, and the guarded get_item-error -> None path). Independent diff-only adversarial review (fresh code-reviewer subagent, saw test + source + conftest): APPROVE WITH NITS, no HIGH; it verified the lxml/jsonpath behaviors directly, confirmed the monkeypatch targets are the references the source actually resolves (incl. that patching `ai_extractor.extract_with_ai` is correct for the call-time `from ai_extractor import` rebind), and confirmed the get_item-error test targets the guarded path. Its one MEDIUM (the AI tests `import ai_extractor`, which does a top-level `import anthropic`, so they would error rather than skip in a partial env lacking that optional dep) was folded in before merge by gating `TestProcessQueriesAi` on a `HAS_ANTHROPIC` flag (mirrors the existing `HAS_SIGALRM` guard; inert under `uv sync` where the tests run + pass); its two LOWs (the AI-failure test does not distinguish inner vs outer except; the regex tests stub `safe_regex_findall` so the `str(page_content)` arg coupling is unverified) were dismissed as the observable contract being what is pinned and the isolation being intentional/honestly commented. No web build (Python backend; tests are the gate). Evidence: new file 29 passed / 3 skipped; full backend unit suite 641 passed / 3 skipped / 0 failed (was 612; +29). Squash-merged into LOCAL claude-main; claude-main now 35 commits ahead of origin/main, LOCAL ONLY (push-free loop; Alex pushes claude-main + opens the claude-main -> main PR himself).
- Characterization tests for `proxy_manager.py` (claude-main, no source change, advances #38 for the core-scrape-engine modules). `AWSProxyManager` selects/rotates/tracks the EC2/Squid proxy pool the 4-tier scraper uses for IP-blocked targets (Tier 2) but had ZERO dedicated coverage despite being the abuse/reliability boundary for outbound traffic: a silent regression either routes scrapes through unhealthy or wrong-geo proxies (bans, blocked jobs) or leaks proxy credentials into logs. Added `backend/tests/unit/test_proxy_manager.py` (36 deterministic tests, no moto, fakes injected for the module-level secretsmanager/cloudwatch clients and the DynamoDB table in the test_rate_limiter style) pinning: `_get_proxy_id` IP-from-credentialed-URL extraction + fallback to the full URL when there is no `@` or a non-IP host; `_mask_proxy_url` credential masking with the security property that the password never survives into the masked log string (and no-credential URLs pass through unchanged); `get_proxy_url` enabled-gate short-circuiting before any pool load, the geo filter requiring BOTH region match AND `status==healthy` (and `any` requiring only healthy), unknown-geo matching nothing, fallback-to-direct returning None vs `fallback_to_direct: False` raising, and the random (default) / round-robin / unknown->first rotation branches each handed the filtered healthy list; `get_proxy_pool` Secrets Manager fetch, the 5-minute cache short-circuiting a second fetch, the zero-TTL forced re-fetch, and the error fallback returning the stale cache when present vs `[]` when not; `_get_round_robin_proxy` lowest-usage selection with missing/erroring usage rows scored 0 and the outer-failure (a proxy dict missing `url`) falling back to `random.choice`; `track_usage`/`mark_proxy_failed` writing the right `proxy_id` Key + counters (`:failed` 0 on success, 1 on failure) and the matching `SuccessfulRequests`/`FailedRequests` CloudWatch metric, both swallowing a DynamoDB error rather than breaking the scrape; `get_proxy_stats` defaults + scan-error->`[]`; and the `get_proxy_manager` singleton. Independent diff-only adversarial review (fresh code-reviewer subagent, saw test + source): APPROVE, no HIGH/MEDIUM false-greens; it empirically verified every regex, the cache-guard math with `_cache_ttl=0`, the KeyError propagation in the round-robin outer-failure path, and the track_usage counter/metric assertions, then instrumented the stale-cache test to confirm the error path is genuinely hit. Its three LOW notes (assert the error path is reached via `bad.calls==1`; lock the non-empty-pool precondition the zero-TTL test relies on; assert the round-robin picker receives the filtered pool) were all folded in before merge. No source change (regression hardening). No web build (Python backend; tests are the gate). Evidence: new file 36 passed; full backend unit suite 612 passed / 0 failed (was 576; +36). Squash-merged into LOCAL claude-main; claude-main now 33 commits ahead of origin/main, LOCAL ONLY (push-free loop; Alex pushes claude-main + opens the claude-main -> main PR himself).

### Recent -- 2026-06-12
- **Google OAuth approach refined + partially provisioned (Alex).** The 2026-06-11 "reuse the client" decision was refined to **reuse the existing GCP project but mint a NEW dedicated OAuth client ID for SnowScrape.** Rationale: a shared client ID would put the same `client_secret` in both `sf-snowscrape` and SnowPipe's Doppler (rotation/blast-radius entanglement); a new client in the SAME project gives SnowScrape its own credentials while still reusing the project's already-enabled Drive/Docs APIs and consent screen (so no new-project verification cycle). Note: API enablement is project-wide (cannot be scoped per client) but is harmless to SnowPipe, since access is gated by per-client requested scopes + user consent, and SnowPipe's GMC client never requests Drive/Docs. **DONE by Alex:** Drive + Docs APIs enabled on the project; new client ID created; `client_id`/`client_secret` added to Doppler `sf-snowscrape`. **REMAINING (Alex):** register SnowScrape's redirect URIs on the new client (the frontend callback page `…/dashboard/integrations/google/callback` on `scrape.snowforge.dev` + the localhost dev one), add `drive.file`+`documents` to the project consent screen (trivial in Testing mode; may need re-verification if Published), set `GOOGLE_OAUTH_REDIRECT_URI` in `sf-snowscrape` dev/prod, then `sst deploy` dev+prod and smoke-test the Connect-Google flow. Stale doc to fix: `docs/INFRASTRUCTURE.md` shows a wrong backend `execute-api` redirect-URI example.
  - **UPDATE (2026-06-12, later) - `documents` scope DEFERRED, deploy HELD.** Alex registered the redirect URIs, added the `drive.file` scope, and set `GOOGLE_OAUTH_REDIRECT_URI` in Doppler, but deliberately deferred the `documents` scope: it is sensitive, and on this shared project (consent screen likely Published for SnowPipe's live GMC) adding it would force a project-wide re-verification that disturbs SnowPipe. `documents` is exactly what writes doc content (`docs.documents().batchUpdate` in `_create_doc`), so the Docs export creates an empty Doc then 403s without it -- the feature is non-functional until the scope lands. Therefore step 4 (`sst deploy`) is ON HOLD. Resume options: add `documents` (accepting the verification implication), stand up a separate Testing-mode GCP project for SnowScrape's Docs OAuth (grant it free, no SnowPipe impact), or re-scope to drive.file-only via a Drive upload-convert rewrite. Not blocking SnowScrape's launch -- Google Docs is a secondary destination.
- Characterization tests for `rate_limiter.py` (claude-main, no source change, files #38 for the remaining core-scrape-engine modules). `DomainRateLimiter` enforces a minimum delay between requests to the same domain (netloc) so scrapes do not flood a target or trip its bot protection; it is on the live scrape path (`crawler.py`, `job_manager.py`, `tiered_scraper.py`, `validators.py` all use it) but had ZERO dedicated coverage. A silent regression is a reliability/abuse risk either way: too short a delay floods the target (IP bans), too long hangs the Lambda. Added `backend/tests/unit/test_rate_limiter.py` (30 deterministic tests driving a `FakeClock` that returns a controllable instant and advances on `sleep` instead of actually sleeping, so the suite is 0.16s and never blocks) pinning: ctor validation (negative `min_delay` -> ValueError, zero allowed as the "disabled" config); `get_domain` netloc extraction incl. the classic gotchas (port and userinfo are part of the netloc so they make distinct "domains"; a schemeless `example.com/path` and a path-only `/x` both yield `""` because urlparse treats them as paths); the `min_delay <= 0` short-circuit returning 0 AND recording no per-domain state (the distinguishing pin: without the early return the fall-through would record `time.time()` for the domain); first-request-to-a-domain no-wait (the real `defaultdict(float)` 0.0 default, asserted explicitly so the test doesn't lean on the FakeClock start); in-window throttling sleeping only the REMAINING time (`min_delay - elapsed`) then recording the post-sleep instant; the `elapsed == min_delay` boundary NOT sleeping (`<` not `<=`); per-domain independence (different netloc/port not throttled, same netloc/different path throttled); and `reset` (single domain, unknown-domain no-op, all). Independent diff-only adversarial review (fresh code-reviewer subagent, saw test + source): APPROVE, no HIGH/MEDIUM, mutation-tested that every load-bearing pin genuinely fails when its source line is broken; its three LOW notes (two disabled-limiter tests over-claimed pinning the short-circuit; the first-request invariant was implicit; a guard comment on the monkeypatch target) were all folded in before merge. No source change (regression hardening). No web build (Python backend; tests are the gate). Evidence: new file 30 passed; full backend unit suite 576 passed / 0 failed (was 546; +30). Squash-merged into LOCAL claude-main; claude-main now 29 commits ahead of origin/main, LOCAL ONLY (push-free loop; Alex pushes claude-main + opens the claude-main -> main PR himself).
- Characterization tests for `webhook_delivery_handler.py` (claude-main, no source change, closes #36 on merge to main). The SQS-triggered outbound webhook Lambda had ZERO dedicated tests despite being the outbound integration security surface: it HMAC-SHA256-signs every event so receivers can verify authenticity, runs the user-supplied target URL through the SSRF validator (`validate_scrape_url`) before any HTTP call, and reports per-record results to SQS via `batchItemFailures` so only failed deliveries retry. A silent regression is either a security hole (forgeable/absent signature, or an SSRF bypass to internal services like the 169.254.169.254 metadata endpoint) or a reliability bug (a failed delivery never retried, or a delivered-but-5xx one retried forever). Added `backend/tests/unit/test_webhook_delivery_handler.py` (35 tests, parametrized over status codes) pinning: SSRF guard blocks `localhost` and the link-local metadata IP BEFORE any `requests.post` (verified offline, no DNS, via the validator's literal-localhost + IP-block paths); the `X-Snowscrape-Signature` header equals `sha256=<hmac_sha256(secret, exact_serialized_body)>` computed over the same body that is POSTed; all event headers (Content-Type, User-Agent, Event, Delivery-ID, unix Timestamp) are set; no signature header when the secret is empty; the 30s timeout; 2xx (200/201/204/299) -> success with `error=None`, non-2xx (301/302/400/404/500/503) -> failure with the real status code and `error` still `None` (a delivered response is not an exception); response body truncated to 1000 chars and `None`/empty normalized to `''`; Timeout/ConnectionError/unexpected exceptions all caught and returned as handled `(False, 0, '', msg)` rather than propagated; and the SQS orchestration -- a success reports no `batchItemFailures`, a failed delivery returns its `messageId` for retry, a malformed-JSON body or a message missing a required key is caught per-record (not crashing the batch) and queued for retry, a mixed batch fails ONLY the failed record's id, and a failure is still logged + stat-counted before retry. The two DynamoDB writers run against the conftest moto tables: `log_delivery` writes the row with a 30-day TTL (`ttl - timestamp == 2592000`) and normalizes `None` body/error to `''`; `update_webhook_stats` increments only `total_deliveries` on success and both `total_deliveries` + `failed_deliveries` on failure, accumulates across calls, and both writers swallow a DynamoDB failure without propagating (best-effort logging must never break delivery handling). No source change (regression hardening). No web build (Python backend; tests are the gate). Evidence: new file 35 passed; full backend unit suite 546 passed / 0 failed (was 511; +35).

### Recent -- 2026-06-11
- **DECISION (Alex): reuse the existing SnowForge Google Cloud OAuth client for the Google Docs destination** instead of standing up a fresh GCP project. This resolves the long-open `new-project-vs-reuse` call that gated the Google Docs destination going live. Net effect: no new OAuth consent-screen verification cycle; SnowScrape's Drive/Docs OAuth rides the already-configured client. Remaining work is Alex-only console + secrets (enable Drive + Docs APIs on that project, add SnowScrape's callback redirect URI + `drive.file`/`documents` scopes, put `client_id`/`client_secret` in Doppler `sf-snowscrape`) plus the backend deploy. No code change required for this decision.
- Characterization tests for `api_key_handler.py` (claude-main, no source change). The programmatic-API key lifecycle module (the "sub-project #2" launch feature) had ZERO dedicated tests despite gating all API-key auth: `create/list/delete_api_key_handler` and, most critically, `validate_api_key` (the chokepoint `utils.resolve_user_id` calls to authenticate `sk_live_...` Bearer keys on the /jobs data-plane). A silent regression there is either an auth bypass (a revoked key still authenticating) or broken programmatic access. Added `backend/tests/unit/test_api_key_handler.py` (24 tests) pinning the security-relevant contracts: plaintext keys are NEVER persisted (stored row carries only the SHA-256 `key_hash`, never a `key` field; `key_hash == sha256(raw)` and `!= raw`), the `key_prefix` is `raw[:12]`; the 10-key-per-user cap with both directions of the boundary (10 existing -> 400 and writes nothing; 9 existing -> the 10th 201, pinning `>= 10` not `> 10`); name required/length (empty/whitespace/`>100` -> 400, exactly 100 allowed); auth gating (no Bearer token -> 401, `validate_clerk_token` raising -> 401 with its message); list scoping (this user only, `created_at` desc, hash never exposed in a list response); delete ownership + soft-delete (missing id -> 400, not-found -> 404, other user's key -> 403 AND the victim row stays `is_active: true`, owner -> 200 with the row still present and `is_active: false`); and `validate_api_key` itself: `None` on empty/None/missing-`sk_live_`-prefix/`Bearer `-led, `None` on unknown hash, `None` on an inactive key, `None` when the row lacks `is_active` entirely (the `.get("is_active", False)` default treats it as inactive), a valid active key returns `{user_id, api_key_id}` and stamps a fresh ISO `last_used_at`, the `last_used_at` update is fire-and-forget (a raising `update_item` does NOT fail validation), plus an end-to-end create -> validate-ok -> delete -> validate-None lifecycle and a CORS guard (an origin outside the allowlist is never reflected, since responses also send `Allow-Credentials: true`). Independent diff-only adversarial review (fresh code-reviewer subagent, saw test + source + conftest): APPROVE, no HIGH/MEDIUM false-greens; it empirically confirmed each security guard genuinely fails if removed and that moto's `Select="COUNT"` on the GSI is immediately consistent so the cap test is not flaky. Its two MEDIUM coverage gaps (cap boundary not pinned; `last_used_at` non-fatal branch uncovered) and the cheap LOWs (ISO-timestamp shape, CORS non-reflection, assert delete 200 in the lifecycle test) were ALL closed before merge (+3 tests, 21 -> 24). Evidence: new file 24 passed; full backend unit suite 511 passed / 0 failed (was 487; +24). No web build (Python backend; tests are the gate). Squash-merged into LOCAL claude-main (8c085ad); claude-main now 26 commits ahead of origin/main, LOCAL ONLY (push-free loop; Alex pushes claude-main + opens the claude-main -> main PR himself).
- Resolved the lint toolchain peer mismatch (claude-main, PR pending, closes #31 on merge to main). `eslint-config-next@16.2.7` pulled `typescript-eslint@8.54.0` transitively, whose peer range is `typescript >=4.8.4 <6.0.0`, but the project resolves `typescript@6.0.3` (caret-pinned in #17), so `pnpm install` printed unmet-peer warnings for `@typescript-eslint/utils`, `/eslint-plugin`, and `/type-utils` and the lint parser was officially unsupported on the TS version in use (latent: lint passed, but a future preset/parser bump or a TS-6-only syntax feature could surface a silent parse gap). Fix (config-only, no app code): added a `pnpm.overrides` block to the ROOT `package.json` forcing the `typescript-eslint` family (`typescript-eslint`, `@typescript-eslint/eslint-plugin|parser|utils|type-utils`) to `^8.61.0` (latest 8.x; peer `typescript >=4.8.4 <6.1.0`, which admits 6.0.3), and regenerated `pnpm-lock.yaml`. Chose a pnpm override rather than a direct devDep on purpose: `frontend/eslint.config.mjs` deliberately reuses the single preset-registered `@typescript-eslint` plugin instance, and a divergent second physical copy would trip ESLint's "Cannot redefine plugin" guard. The override cascades to all six internal subpackages (`scope-manager`/`types`/`typescript-estree`/`visitor-keys`/`project-service`/`tsconfig-utils`), which the family pins to exact matching versions, so the lockfile now has zero `8.54.0` and exactly one `8.61.0` copy of each. The caret keeps the project on the 6.x TS line (honoring #17) and excludes a future regression to the `<6.0.0`-peer line. Only incidental lockfile movement: `@types/node` 25.9.2->25.9.3 (a benign patch within its caret) plus the new family's own transitive deps (`ts-api-utils`, `eslint-visitor-keys`, `minimatch` inside `typescript-estree`); no runtime dep (next/react/vitest/eslint/typescript) version changed, and the root SST workspace (`typescript: latest`) is unaffected (same single TS 6.0.3 copy; SST does not depend on the ts-eslint family). Independent diff-only adversarial review (fresh code-reviewer subagent): APPROVE, no HIGH/MEDIUM (its one optional LOW, a `@typescript-eslint/*` wildcard override, dismissed as redundant since the family already cascaded). Evidence: `pnpm install --frozen-lockfile` exit 0 with 0 unmet-peer warnings; `eslint .` 0 errors / 106 baseline warnings (unchanged); `vitest run` 121/121 passed; `doppler --project sf-snowscrape --config dev -- pnpm build` exit 0 (Compiled successfully in 6.6s, 27/27 static pages).
- Hardened two latent validator edge-cases in `backend/validators.py` (claude-main, PR pending, closes #35 on merge to main). The prior cycle's coverage work characterized two Python gotchas as current behavior; this cycle fixes the source and inverts the pins. (1) `validate_file_mapping` accepted a bool `url_column` as an int index because `isinstance(True, int)` is `True`, so `True`/`False` were silently coerced to index `1`/`0`; now a `bool` is rejected with "URL column must be an integer index or string name" via an explicit `isinstance(url_column, bool)` guard before the int branch. (2) `sanitize_string(max_length=0)` skipped the length check entirely because `if max_length and ...` treats `0` as falsy; changed to `if max_length is not None and ...` so a zero limit enforces "no characters allowed" (rejects non-empty input, still allows `""`). Both are defense-in-depth on the live job-create validation path (`validate_job_data_strict`); `sanitize_string` currently has no production callers. TDD: the two characterization tests were rewritten to assert the stricter behavior (`test_rejects_bool_url_column` parametrized over `True`/`False`; `test_max_length_zero_rejects_non_empty_input` + `test_max_length_zero_allows_empty_string`) and confirmed failing against the old source first. Independent diff-only adversarial review (fresh code-reviewer subagent): APPROVE, no HIGH/MEDIUM; verified the tests genuinely fail on the pre-fix code (no false-greens) and the only real behavior change (bool rejection) affects malformed external input only. Evidence: backend unit suite 487 passed / 0 failed (was 485; net +2), full suite 512. No web build (Python backend; tests are the gate).

### Recent -- 2026-06-10
- Test coverage for the previously-untested job-config validators (claude-main, PR pending; no source change). `backend/validators.py` (895 loc, the input-validation layer on the live job-create path) had a dedicated security suite (`test_security.py`: SSRF, XPath whitelist, ReDoS, job-name/url-list bounds, `validate_job_data_strict`), but five `InputValidator` methods had ZERO direct coverage: `validate_jsonpath_query`, `validate_file_mapping`, `sanitize_string`, `validate_url_template`, and `validate_source_type`. These gate real job configs (jsonpath query type, CSV column mapping, scheduled/`direct_url` templated targets, generic string scrub), so a silent behavior change would have passed CI. Added `backend/tests/unit/test_validators_input.py` (60 pure/deterministic characterization tests) pinning each method's contract: jsonpath `$`/`@` rooting + balanced-bracket + length guards; file-mapping required-key, delimiter/enclosure/escape whitelists and url_column int-range (0-100) / named-column / wrong-type rules; sanitize_string control-char stripping (newlines/tabs preserved), trim, and optional max_length; url_template empty/length/non-http-scheme/malformed-variable rejection plus plain and templated acceptance; source_type `csv`/`direct_url` allowlist. No source change (regression hardening); every method behaved exactly as written (all green on first run). An independent diff-only adversarial review (fresh code-reviewer subagent) returned APPROVE with no HIGH/MEDIUM correctness defects; its MEDIUM coverage-gap notes were addressed by adding 5 more pins for the classic Python gotchas it flagged (bool-is-int url_column accepted as an index, falsy max_length=0 disabling the length check, \r stripping, and the url_template no-hostname + unknown-variable branches). Evidence: new file 65 passed; full backend suite 510 passed / 0 failed (was 445; +65), unit-only 485 (was 420).
- Dropped Parquet as an offered export format (claude-main, PR pending, closes #33 on merge to main). The JobCard download menu offered Parquet, but Parquet needs `pyarrow`, which is intentionally NOT bundled in the Lambda (size / cold-start cost, decided in the #32 rewrite), so every Parquet download failed 100% of the time in prod. Issue #33 framed this as "bundle pyarrow vs drop the format"; since the no-bundle decision is already made, dropping it is the logical consequence, not a new product call. Changes: extracted the offered set into a pure, testable `frontend/lib/jobs/exportFormats.ts` (`EXPORT_FORMATS` = json/csv/xlsx/sql, with an `ExportFormat` type) and wired `JobCard` to it; tightened the `jobsAPI.download` format union to drop `'parquet'`; removed `'parquet'` from the backend download handler's `valid_formats` so a Parquet request now returns a clean 400 ("Invalid format") instead of reaching the converter's handled RuntimeError -> 500; and scrubbed Parquet from every user-facing surface that advertised it as available (home, features, getting-started, exporting-data docs incl. the dedicated card + comparison-table row, docs/api/jobs, `docs/API.md`, `backend/openapi.yml` both enums + description, README). `convert_to_parquet` stays in `format_converter.py` as dormant code (and keeps its handled-error test), so re-enabling later is just bundling pyarrow + re-adding 'parquet' to `EXPORT_FORMATS` and `valid_formats`. TDD: new `frontend/lib/__tests__/jobs/exportFormats.test.ts` (3 tests: exact offered set, Parquet excluded, every format has a label+description). Evidence: frontend vitest 121 passed / 0 failed (was 118; +3); backend unit suite 420 passed / 0 failed (the existing pyarrow-missing converter test still green); `eslint .` 0 errors / 106 baseline warnings; `doppler --config dev -- pnpm build` exit 0 (Compiled successfully in 8.8s, 27/27 static pages). Backend behavior change reaches prod only on the next backend deploy (Alex-only), but the UI/marketing fix ships with the frontend; until the backend redeploys, a direct API Parquet request still hits the old 500 path rather than the new 400, which is a strict improvement either way (no longer offered in the UI).
- Spreadsheet formula-injection hardening for CSV/XLSX exports (claude-main, PR pending, closes #34 on merge to main). Export cells are arbitrary scraped (attacker-influenced) web content; `backend/format_converter.py` wrote them verbatim, so a value beginning with `=`, `+`, `-`, `@`, tab, or CR (e.g. `=HYPERLINK("http://evil","click")`) is evaluated as a formula when the export is opened in Excel / Google Sheets / LibreOffice (CSV/spreadsheet formula injection, CWE-1236). Pre-existing (the old pandas converter had the same gap; surfaced during the #32 rewrite). Fix: new `_sanitize_spreadsheet_text` helper prefixes a single quote when the stringified value starts with a trigger char, applied to the CSV write path and to string-valued XLSX cells (numbers/bools stay native so they remain numeric in Excel; openpyxl would otherwise store a leading-`=` string as a real formula cell). The header row is sanitized too (column names come from scraped/AI-derived JSON keys). SQL and Parquet are intentionally untouched: SQL emits single-quote-escaped string literals (not formula-evaluated) and Parquet is a binary data format. TDD: 12 new tests in `backend/tests/unit/test_format_converter.py` (parametrized CSV payloads incl. `=`/`+`/`-`/`@`/tab/CR, benign-values-unchanged, XLSX formula cell `data_type != 'f'` with native numbers preserved, CSV+XLSX header sanitization, and a scope guard that SQL output is NOT spreadsheet-quoted). Independent diff-only adversarial review (fresh code-reviewer subagent) flagged one HIGH (the header row was initially unsanitized); fixed before merge, plus added the header regression + SQL scope-lock tests it requested. Evidence: full backend suite 445 passed / 0 failed (was 433; +12). Reaches prod on the next backend deploy (Alex-only), same deploy as the #32 pandas-free converter.
- Multi-format export made pandas-free so non-JSON downloads stop crashing in prod (claude-main, PR pending, closes #32 on merge to main). `backend/format_converter.py` imported `pandas` and `openpyxl` at module top, but pandas is intentionally NOT bundled in the Lambda (same fact behind the 2026-06-08 CSV-parsing fix), and the download handler imported the converter and called `prepare_dataframe()` OUTSIDE its conversion `try/except`. So the first non-JSON download of an uncached job raised an unhandled `ModuleNotFoundError`: CSV, XLSX, Parquet, and SQL exports were all broken in prod (only JSON worked), despite "Multi-Format Export" reading Complete. Fix: rewrote the converters pandas-free. CSV (stdlib `csv.DictWriter`) and SQL (manual writer) now work with zero heavy deps and are directly testable; XLSX uses `openpyxl` (a pure-Python wheel, added to `backend/pyproject.toml` so it bundles); Parquet still needs `pyarrow` (too large to bundle), so it raises a clear, handled `RuntimeError` instead of crashing. `prepare_dataframe` now builds rows + a first-seen column union (matching the old `pd.DataFrame(list_of_dicts)` ordering) and the module resolves S3 lazily via `connection_pool.get_s3_client()` (the old module-level `boto3.client('s3')` pinned credentials at import and broke under moto). Also moved the converter import + `prepare_dataframe()` inside the handler's conversion guard so any failure returns a clean 500. SQL identifiers are now backtick-escaped (scraped JSON keys are untrusted) and empty results emit a SQL comment instead of invalid `CREATE TABLE ... ()`. TDD: new `backend/tests/unit/test_format_converter.py` (14 tests: CSV/SQL/XLSX round-trips via moto, prepare flatten/non-flatten column union, None->''/NULL, list/dict scalarization, batch-of-100, backtick-escaping, empty-results, the pyarrow-missing RuntimeError path, and a no-pandas import guard). Independent diff-only adversarial review (fresh code-reviewer subagent) APPROVE, no HIGH; its two MEDIUMs (backtick identifier injection + invalid empty-table DDL) both fixed before merge. Evidence: full backend suite 433 passed / 0 failed (was 419; +14); pandas + pyarrow confirmed absent in the env (matches prod), openpyxl present. NOTE: the openpyxl dep + the pandas-free converters reach prod only on a backend deploy (Alex-only); until then prod still 500s on non-JSON downloads.
- Test coverage for the URL-variable resolver (claude-main, PR pending). `backend/url_variable_resolver.py` (429 loc, the PHP-style `{{date}}`/`{{time}}` templating that expands scheduled-scraping `url_template`s) had ZERO dedicated tests, despite being on the live job-create/run path (`utils.resolve_url_template`/`preview_url_template` and `validators.JobValidator.validate_timezone` both call into it). Added `backend/tests/unit/test_url_variable_resolver.py` (53 tests, pure/deterministic against a fixed `exec_time`) pinning every documented behavior: default + custom PHP formats (`Y-m-d`, `m/d/Y`, `Y-m-d_h_iA`, 2-digit year), time offsets (`+1d`/`-1d`/`+2h`/`-30m`/`+15s`, day rollover), no-leading-zero specifiers (`n`/`j`/`G`/`g`) vs padded (`m`/`d`/`H`/`h`), lowercase vs uppercase meridiem (`a`/`A`), the 12-hour midnight/noon wrap (00->12 AM, 12->12 PM), timezone resolution (UTC->America/New_York shifts the hour and can roll the date back) with graceful fall-back to UTC on an invalid tz, `has_variables`/`extract_variables`, `validate_template` (empty, unmatched braces, bad variable name, literal-passthrough format chars) and `validate_timezone`, the defensive-copy contract of `get_common_timezones`, and the full `preview()` payload (valid, invalid-template, invalid-timezone, and tz-reflected `resolved`/`resolved_at`). No source change (characterization/regression hardening); the module behaved exactly as documented (all 53 green on first run). Evidence: new file 53 passed in 2.12s; full backend suite 419 passed / 0 failed (was 366; +53).
- openapi.yml `servers` now point at the real API Gateway hosts (claude-main, PR pending, closes #24 on merge to main). The published spec (referenced from `docs/API.md`, the programmatic-API launch surface) shipped an unusable `servers` block: a `localhost:3000` entry (misleading, the backend is only reachable via API Gateway, and the dev server binds 3001 anyway) and a templated `https://{apiId}.execute-api.us-east-2.amazonaws.com/{stage}` whose `apiId` defaulted to `REPLACE_ME`, so SDK generators and Swagger UI "Try it out" resolved to a dead host. Fix (docs/spec-only, no runtime change): replaced the block with two concrete bare-host servers, Production `https://2pg2gj4048.execute-api.us-east-2.amazonaws.com` (listed first, the default) and Development `https://g5vmashyda.execute-api.us-east-2.amazonaws.com`. These are HTTP API (V2) gateways on the auto `$default` stage (`sst.config.ts` `ApiGatewayV2`, routes registered as `POST /jobs` etc.), so routes are served at the host root with NO stage path prefix; the URLs match `frontend/.env.example` `NEXT_PUBLIC_API_BASE_URL` (the working client) and `docs/INFRASTRUCTURE.md`. Also corrected `docs/API.md`, whose base-URL example wrongly carried a `/prod` suffix (inconsistent with its own `$BASE_URL/jobs` curl examples). Extended the `test_openapi_spec.py` drift guard with three tests: no `REPLACE_ME`/placeholder in any server URL or variable default; servers resolve to the real prod gateway and include no localhost host; and no server URL carries a stage path segment (root-served V2 API). Deferred (issue #24 point 3, larger): emit the server URLs from SST stack outputs so they cannot drift per redeploy. Self-review caught the initial draft's incorrect `/prod`//`dev` suffixes before merge. Evidence: full backend suite 366 passed / 0 failed (was 363; +3 server-block guard tests). Frontend untouched.

### Recent -- 2026-06-08
- Pinned all frontend dependencies + enforced a frozen lockfile in CI (claude-main, PR pending, closes #17 on merge to main). `frontend/package.json` declared ~50 deps as `"latest"` while the Frontend CI workflow installed with `pnpm install --no-frozen-lockfile`, so every CI run re-resolved to whatever was newest and "passes locally" no longer implied "passes in CI". This already broke a build once (`@vitejs/plugin-react: latest` resolved to 6.0.1, which needs Vite 8 and crashed against the Vite 7.3.1 that Vitest 4 bundles). Fix (config-only, no app code): replaced every `"latest"` specifier with a caret range pinned to the exact version currently resolved (e.g. `next ^16.2.7`, `vitest ^4.1.8`, `@clerk/nextjs ^7.4.3`), keeping the already-pinned `@vitejs/plugin-react ^5.1.4` / `eslint ^9` and the `@snowforge/ui` git ref untouched; regenerated `pnpm-lock.yaml` (direct-dependency versions unchanged, verified via `pnpm ls`; some transitive deps re-resolved within their existing ranges); and switched all 4 `frontend.yml` install steps from `--no-frozen-lockfile` to `--frozen-lockfile` (matching `backend.yml`). Evidence: `pnpm install --frozen-lockfile` succeeds against the new manifest (the exact CI check), `eslint .` 0 errors / 106 baseline warnings, frontend vitest 118/118, `doppler --config dev -- pnpm build` exit 0 (Compiled successfully in 10.7s). No runtime or behavior change; upgrades are now intentional (caret within major) instead of silent.
- CSV parse errors now surface instead of being masked (claude-main, PR pending, closes #12 on merge to main). `backend/utils.py` `parse_links_from_file` wrapped the pandas path in a bare `except Exception` that unconditionally fell through to the manual `csv.reader` parser and logged only a WARNING. Because pandas is intentionally not bundled in the Lambda, the pandas branch ALWAYS raised `ImportError` in production, so the manual parser was the only path that ever ran, yet every CSV job logged a misleading "Pandas failed to parse file" WARNING and a genuine manual-parser bug looked identical to that benign warning (this is how the header-row/empty-cell ingestion bug, fixed 2026-06-06, stayed hidden). Fix (backend-only, no runtime behavior change for valid inputs): split the two parsers into `_parse_links_with_pandas` (best-effort, dev/local only) and `_parse_links_manual` (the real production path); `parse_links_from_file` now runs pandas only when `PANDAS_AVAILABLE` and treats its failure as a benign DEBUG fallback, while a failure in the manual parser is logged at ERROR and re-raised so it is never swallowed. A fetch failure now propagates directly instead of triggering an `UnboundLocalError` in the old fallback. Evidence: backend full suite 363 passed / 0 failed (338 unit incl. +3 new error-surfacing tests, 25 integration); pandas confirmed absent in the env (matches prod). Independent diff-only adversarial review APPROVE, no HIGH/MEDIUM; its one LOW (cover the pandas-available-but-fails DEBUG-downgrade path) addressed before merge with a 3rd test.
- DestinationSelector now enforces the 10-destination cap client-side (claude-main, PR #28, closes #9 on merge to main). The backend caps a job at 10 export destinations (`backend/utils.py`: "Maximum 10 export destinations per job"), but `DestinationSelector` let users check unlimited boxes, so the limit was only discovered when job creation failed with a 422. Fix (frontend-only, no runtime/backend change): a `MAX_DESTINATIONS = 10` constant (mirrors and comments the backend cap), unchecked checkboxes disabled once 10 are selected (`cursor-not-allowed opacity-50`) while already-selected boxes stay enabled so they can be unchecked, a `toggle()` guard that refuses to add beyond the cap even if a disabled control fires, and an `N / 10 selected` count plus an `aria-live` "Maximum 10 destinations per job" message at the cap. Improves all three job-creation flows (manual, AI, Visual) that share this selector. Evidence: frontend vitest 118 passed (was 113; +5 cap tests), `eslint .` 0 errors / 106 baseline warnings, `doppler --config dev -- pnpm build` exit 0 (Compiled successfully). Independent diff-only review APPROVE, no HIGH/MEDIUM (its one actionable LOW, aria-live on the cap message, addressed before merge).

### Recent -- 2026-06-07
- CSS-to-XPath translation for Visual/AI builder fields (claude-main, closes #26 on merge to main). The Visual and AI job builders relabeled a `css` field's `type` to `xpath` while leaving the query string in CSS syntax, so the backend XPath engine (`crawl_manager.py` runs only xpath/regex/jsonpath) evaluated a CSS-syntax expression and silently extracted nothing. Fix: new pure `frontend/lib/jobs/cssToXpath.ts` `cssSelectorToXPath()` that translates the common, unambiguous CSS subset (type/`*`, `#id`, `.class`, attribute selectors `[a]`/`[a=v]`/`^=`/`$=`/`*=`/`~=`/`|=`, descendant and child combinators, selector lists) to XPath 1.0 and returns `null` for anything it cannot prove correct (sibling combinators, pseudo-classes, CSS escapes, single-quoted values). Both builders now emit the translated XPath for a `css` field; if translation returns `null`, they throw a clear, field-named error that the existing job-create try/catch surfaces as a toast, so a bad selector fails loudly instead of silently losing data. AI builder's `useAi` (natural-language) path is unchanged and takes precedence. Evidence: frontend vitest 101 passed (was 51; +42 converter tests + builder translate/throw tests), `eslint .` 0 errors / 106 baseline warnings, `doppler --config dev -- pnpm build` exit 0 (Compiled successfully). No backend change; no runtime change for already-xpath auto-extracted fields.
- Visual builder job creation fixed (claude-main, PR #25, closes #8 on merge to main). The Visual Scraper Builder's "Create Job" sent a payload with no `source_type`/`url_template`, so the backend defaulted `source_type` to `'csv'` (`validators.py`) and then rejected the job for a missing `file_mapping` the flow never provides. The builder's primary action always failed validation. Fix: extracted `frontend/lib/jobs/buildVisualJobPayload.ts` (a pure helper mirroring `buildAiJobPayload`) that builds a `direct_url` `CreateJobDTO` with `url_template` set to the target URL, and downgrades `css` field types to `xpath` (the backend job runner accepts `xpath/regex/jsonpath/pdf_*`, not raw `css`). Wired into `visual/page.tsx` `handleSaveAsJob`; added 5 unit tests. Evidence: frontend vitest 56 passed (was 51), `eslint .` 0 errors, `doppler --config dev -- pnpm build` exit 0 (27/27 static pages), PR #25 Vercel preview PASS. Independent diff-only review APPROVE (no HIGH; one MEDIUM test-name fix addressed). Known follow-up (separate issue): a downgraded css field keeps its css-syntax query string labeled `xpath`, so a manually-added css field can extract nothing at runtime (auto-extracted fields are already xpath, so this is rare).
- Programmatic API: documented and spec-pinned the API-key auth that shipped on 2026-06-06 (claude/snowscrape/openapi-apikey-auth-docs, PR into claude-main). The `/jobs` data-plane accepts an `sk_live_...` API key OR a Clerk JWT (via `resolve_user_id`), but `backend/openapi.yml` still said "All endpoints require a valid Clerk JWT" and defined only the `BearerAuth` scheme, so the launch feature (sub-project #2, programmatic access) was undocumented. Changes: (1) rewrote the `info` Authentication section to describe both credential types and which surface accepts each; (2) added an `ApiKeyAuth` security scheme; (3) added `security: [BearerAuth, ApiKeyAuth]` to the 13 data-plane operations (createJob, getAllJobStatuses, getJobDetails, updateJob, deleteJob, pause/resume/cancel/refreshJob, getJobCrawls, getCrawlDetails, downloadResults, previewResults), leaving the control-plane Clerk-only by design; (4) wrote `docs/API.md`, a programmatic quickstart (create key, authenticate, job lifecycle, download, actions, rate limits); (5) added `backend/tests/unit/test_openapi_spec.py`, a drift guard that pins the spec to the real auth model (data-plane ops advertise ApiKeyAuth; control-plane ops do not), plus `pyyaml` as a dev dep. Evidence: full backend suite 360 passed / 0 failed (was 337; +23 from the new guard). No runtime code changed; frontend untouched.

### Recent -- 2026-06-06
- Frontend E2E (Playwright) red-signal fix (claude/snowscrape/fix-e2e-port-quarantine, DRAFT PR into claude-main; issue #18). Root cause of the E2E job failing on PR #7: the `dev` script binds port 3001 (`next dev -p 3001`) but `playwright.config.ts` `webServer.url` + `baseURL` pointed at `http://localhost:3000`, so in CI Playwright spawned the dev server, waited 60s for 3000, never saw it, and the whole job failed before any test ran (`Error: Timed out waiting 60000ms from config.webServer`). Fix: align the config to 3001 (env-overridable `PLAYWRIGHT_PORT`), raise `webServer.timeout` to 120s (cold Next dev + building the `@snowforge/ui` git dep can exceed 60s), and run a single engine (chromium) in CI instead of 5 (the 5-engine matrix at workers:1 + retries was slow/flaky). Also quarantined the auth/backend-dependent specs behind flags (`e2e/helpers/test-env.ts`): the Clerk-gated dashboard specs (billing-flow, notifications, templates, onboarding) run only when `CLERK_TEST_EMAIL`/`_PASSWORD` are set; the strict axe scans (accessibility.spec) run only when `RUN_A11Y=1`. Verified locally with valid Doppler `sf-snowscrape` dev keys: full suite in CI mode = 4 passed, 54 skipped, 0 failed in 9.3s (was a 2m timeout); lint 0 errors / 106 warnings; vitest 51/51; prod build green.
  - REMAINING (Alex-only, ledgered): `<ClerkProvider>` wraps the root layout, so every route, including the public homepage smoke, needs valid Clerk keys to render. CI has none, so the E2E job stays red until `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` + `CLERK_SECRET_KEY` (test instance) are added as GitHub Actions secrets (the Frontend workflow's E2E step now consumes them). Because of this the work branch is left as a DRAFT and NOT merged into claude-main. Optionally add `CLERK_TEST_EMAIL`/`_PASSWORD` + a seeded Clerk test user to also un-skip the authenticated dashboard specs.
- Backend CI red-signal fix (claude-main, pending Alex's merge to main). The `claude-main -> main` PR (#7) `Backend Tests` job failed on `test_delete_job_handler_success` with `NoCredentialsError` (issue #16). Root cause: `utils.py` bound `s3`, `job_table` and `url_table` to connection-pool resources at module IMPORT time, pinning each resource to whatever credentials resolved during import. On CI there are no ambient AWS credentials at import (moto/test creds are set up per-test, after import), so `delete_job_links -> url_table.query` reached real boto3 with no creds. It passed locally only because the dev machine has ambient creds; the import-time binding also defeated the per-test connection-pool reset in conftest. Fix: fetch the URLs table and S3 client lazily at call time via `get_table`/`get_s3_client` (the pattern the rest of the code already uses) in `delete_job_links`, `fetch_urls_for_job`, `update_url_status`, `refresh_job_urls`, `delete_s3_result_file`; removed the dead import-time `job_table` global. Added `TestLazyAwsResourceResolution` regression tests (no import-time AWS globals; helpers resolve their resource at call time). Evidence (local): full backend suite 337 passed (was 334). Decisive verification is PR #7's Backend Tests CI re-run. Merged to claude-main via PR #19. Remaining frontend red signal on PR #7: `E2E Tests` (Playwright, issue #18).
- Frontend CI red-signal fix (claude-main, pending Alex's merge to main). The `claude-main -> main` PR (#7) had the frontend `Unit Tests` and `Lint` checks failing; both were toolchain/config breaks, not product bugs:
  - **Lint:** `pnpm lint` ran `next lint`, which Next.js 16 removed (it parsed `lint` as a project-directory arg and exited 1, so lint had not actually run for some time). Migrated to the ESLint flat config: added `frontend/eslint.config.mjs` (mirrors the prior `next/typescript` ruleset), changed the `lint` script to `eslint .`, and removed `.eslintrc.json`. `no-explicit-any` (75 pre-existing, pervasive in the API/websocket payload layer) is kept as a warning to ratchet down separately; `no-require-imports` is off for config/`.cjs` files where require() is correct; removed one orphaned `react-hooks/exhaustive-deps` disable comment (that rule was never registered under `next/typescript`). Result: `eslint .` exits 0 (0 errors, 106 warnings).
  - **Unit Tests:** CI installs with `--no-frozen-lockfile`, and `@vitejs/plugin-react: latest` resolved to 6.0.1, which imports `vite/internal` (needs Vite 8) and crashed against the Vite 7.3.1 that Vitest 4 bundles (`ERR_PACKAGE_PATH_NOT_EXPORTED`). Pinned `@vitejs/plugin-react` to `^5.1.4` (the proven Vite-7-compatible line already installed locally). Frontend suite stays 51/51 green.
  - Evidence (local): `eslint .` 0 errors / 106 warnings (exit 0); `vitest run` 51 passed; production `next build` succeeded.
- API-key auth wired across the entire /jobs data-plane (claude-main, pending Alex's merge to main). Programmatic API access (the launch "sub-project #2") was blocked because `validate_api_key` existed but was never called: every public endpoint still required a Clerk JWT, so the Settings -> API Keys feature produced keys that did not actually authenticate anything. Added a single auth resolver `resolve_user_id(token)` in `utils.py` that accepts either a SnowScrape API key (`sk_live_...`, checked first by prefix) or a Clerk session JWT, returning the owning user_id (raising on an invalid credential so callers keep their existing 401 path). Wired it into all 13 /jobs data-plane handlers (create, list/status, get, update, delete, pause, cancel, resume, refresh, crawls list+get, download, preview). Control-plane endpoints (api-keys CRUD, billing, integrations/OAuth, templates, webhooks, destinations) intentionally stay Clerk-only. Billing gates are unaffected: they key off user_id, which the resolver supplies for API-key callers too. TDD: 4 resolver unit tests + 2 end-to-end API-key integration tests (valid key authenticates a /jobs read; revoked key -> 401). Evidence: full backend suite 333 passed (308 unit + 25 integration), up from 327.
- Backend red-signal fix (claude-main, pending Alex's merge to main). The backend unit suite was red: 11 failing tests (4 in `test_utils.py`, 7 in `test_ai_extractor.py`). Root causes and fixes:
  - **Real production bug in CSV URL parsing.** `parse_links_from_file`'s pandas path is dead in production (pandas is intentionally not bundled in the Lambda, so it is not in `backend/pyproject.toml` and never imports), meaning the manual `csv.reader` fallback is the ONLY path that runs in prod. That fallback did not skip the header row, did not skip empty cells, and did not support the `'default'` auto-detect column option, so every CSV-sourced job ingested the header text as a bogus URL and could not auto-detect a URL column. Rewrote the fallback to mirror the pandas semantics (row 0 = header, resolve the column against the header, skip the header row and empty cells, support `'default'` via `detect_url_column`). 3 previously-failing `TestParseLinksFromFile` tests now encode the correct behavior and pass.
  - **Stale auth test.** `test_extract_token_case_sensitive` asserted a lowercase `authorization` header yields no token, but `extract_token_from_event` deliberately accepts it (API Gateway V2 lowercases all header names). Corrected the test to match the intended, correct behavior.
  - **AI-extractor test mock drift.** `test_ai_extractor.py` patches `ai_extractor.anthropic`, but the module lazy-imported `anthropic` inside `_get_client`, so the attribute did not exist at patch time (7 failures). Moved `import anthropic` to module scope (it is already a hard dependency); client construction stays lazy. No behavior change.
  - Evidence: backend unit suite 304 passed / 0 failed (was 293 passed, 11 failed); integration suite 23 passed.

### Recent -- 2026-06-05
- Frontend: wired the export-destination selector into the AI-assisted and Visual builder job-creation flows. Previously only the manual form rendered `DestinationSelector`; jobs created via AI/Visual silently dropped any chosen destinations and could never auto-export. Added `export_destination_ids` (plus `source_type`, `url_template`) to `CreateJobDTO`, extracted the `buildAiJobPayload` pure helper with 6 unit tests, and rendered `DestinationSelector` in both flows. Frontend suite 51/51 green. Merged to `claude-main` via PR #6; awaiting Alex's `claude-main -> main` review.

### Recent -- 2026-06-03
- Backend fix: `job_manager.py` now propagates `results_s3_key` into `job_data` before the docs-export fan-out (without it, the export Lambda had no S3 results to read). Committed + pushed to `main`, alongside the Google Docs destination implementation plan and a user-action checklist (`docs/superpowers/plans/2026-06-01-google-docs-destination.md`, `2026-06-02-google-docs-destination-user-checklist.md`).
- Walked Alex through the Google Cloud Console OAuth setup (create project, enable Drive + Docs APIs, configure consent screen, create OAuth client). This is step 1 of the user checklist and the active remaining blocker before the Google Docs destination is live in prod. Context: scoped while evaluating a LinkedIn-post-scraping freelance gig.

---

## What's DONE

### Infrastructure (SST Ion Migration -- Complete)
- Migrated from Serverless Framework to SST Ion v3 (TypeScript IaC, Pulumi engine)
- Merged frontend + backend into unified monorepo with pnpm workspaces
- Migrated API Gateway from V1 (REST) to V2 (HTTP) -- lower latency and cost
- Migrated Python deps from pip/requirements.txt to uv/pyproject.toml
- 29 Lambda functions deployed and operational
- 8 DynamoDB tables with PAY_PER_REQUEST billing, SSE encryption, PITR
- 4 SQS queues (job + webhook, each with DLQ)
- S3 results bucket with versioning and Glacier lifecycle
- Secrets moved from hardcoded to SSM Parameter Store
- CORS restricted to stage-specific origins
- SSRF protection, XPath whitelist, regex timeout validation
- CI/CD via GitHub Actions (frontend + backend workflows)

### 4-Tier Scraping System (Complete)
- Tier 1: Direct HTTP requests (static HTML)
- Tier 2: Residential proxy rotation (IP-blocked sites)
- Tier 3: Firecrawl JS rendering (JavaScript SPAs)
- Tier 4: Firecrawl anti-bot bypass (bot-protected sites)
- Automatic escalation on failure

### AI-Powered Extraction (Complete)
- Claude integration via `ai_extractor.py`
- Natural language queries against scraped content
- Structured data extraction from unstructured HTML

### Real-Time WebSocket (Complete)
- API Gateway V2 WebSocket API
- Live job progress updates
- Connection management with DynamoDB (TTL-enabled)

### Webhook System (Complete)
- Event-driven notifications (job.created, started, completed, failed, cancelled)
- SQS-based async delivery with retry logic (max 3 attempts)
- HMAC SHA256 signatures for security
- Delivery tracking and DLQ for failed deliveries
- Webhook management UI

### Template System (Complete)
- Save and reuse scraping configurations
- Template CRUD with user-scoped access

### Visual Job Builder (Complete)
- No-code job creation UI
- Query builder for XPath, Regex, JSONPath, CSS selectors

### Multi-Format Export (Code-complete; CSV/XLSX/SQL pandas-free on claude-main, awaiting backend deploy)
- JSON (live), CSV, XLSX, SQL export (rewritten pandas-free 2026-06-09, #32; CSV/XLSX formula-injection sanitized 2026-06-10, #34; reach prod on the next backend deploy)
- Parquet: DROPPED as an offered format 2026-06-10 (#33). It needs pyarrow, which is intentionally not bundled in the Lambda (size / cold-start), so it failed 100% of the time. Removed from the UI, client type, backend `valid_formats` (now a clean 400), and all advertising copy. `convert_to_parquet` remains dormant in `format_converter.py`; re-enable later by bundling pyarrow and re-adding the format.
- Server-side conversion with S3 caching

### Frontend (Complete)
- Next.js 16 with App Router
- Tailwind CSS 4.x with semantic color tokens (dark mode)
- Radix UI component library
- Collapsible sidebar layout (reference implementation for SnowForge LLC)
- Marketing landing pages
- Sentry error monitoring
- 42 unit tests (Vitest), Playwright E2E setup

### Google Docs Export Destination (Backend complete; code-ready for deploy)
- OAuth flow for connecting user Google accounts (drive.file + documents scopes; drive.metadata.readonly deferred to v1.1 folder picker)
- KMS-encrypted refresh token storage (per-user)
- Export destinations CRUD (Google Docs as v1 target)
- SQS-triggered docs export Lambda: reads scrape results from S3, formats via three templates (structured_log, compact_list, narrative), writes to user's Drive folder via Google Docs batchUpdate API
- Fan-out from job completion handler: jobs can attach 0-10 destinations; results auto-export after each successful run
- Frontend UI: Integrations page (connect/disconnect Google), Destinations CRUD page, DestinationSelector inside the manual job creation form
- 50+ backend unit + integration tests covering OAuth, destinations, dispatcher, and export Lambda
- Deploy + Doppler secrets + Google Cloud Console OAuth client setup are user-action follow-ups
- Drive folder picker UI deferred to v1.1 (users paste folder ID for now)
- AI/Visual job creation forms now wired with destinations (2026-06-05, PR #6); manual form already had it

### Billing / Stripe Integration (Live in production)

- 14-day Pro trial with card-required checkout (no free starter tier)
- Stripe Customer Portal handles all plan changes, cancellations, payment updates (no in-app plan picker)
- 3 paid tiers: Pro $49/mo, Business $149/mo, Enterprise (sales-led)
- Backend: idempotent Stripe webhook handler with `BillingWebhookDedup` table, race-fixed period-end updates, hard 402 gate at job-create when subscription inactive, fail-open usage counters
- Frontend: subscription-status proxy gate (60s cookie cache, only caches `trialing`/`active`), `/onboarding/checkout` and `/billing/locked` screens, Settings → Billing tab live data, one-time-secret API-key modal
- Pricing page: dropped Starter card, 14-day-trial hero, `<PricingCTA>` routes signed-out → /sign-up, signed-in active → portal, signed-in no-sub → /onboarding/checkout
- Tests: 11 backend webhook + integration tests, 3 Vitest unit tests, 1 Playwright E2E (gated on Clerk test user)
- **Deployed to dev** (test mode) on 2026-05-07: smoke test passed end-to-end (signup → checkout → trial → cancel → past-due lockout → webhook idempotency replay)
- **Deployed to prd** (live mode) on 2026-05-08: backend + frontend live; awaiting first real $0 trial signup as final acceptance
- Live Stripe identifiers (test mode): Pro `price_1TUYHnAnsCk0eFqBMfkFoAaj`, Business `price_1TUYHzAnsCk0eFqBcXokmvKv`, Portal config `bpc_1TUYIIAnsCk0eFqBGb41J2Zv`, Webhook `we_1TUat5AnsCk0eFqBGBqPX87U`
- Live Stripe identifiers (live mode): Pro `price_1TUf3bAhxqX4McFQyyCQu6Tq`, Business `price_1TUf3mAhxqX4McFQlhAp2mCF`, Portal config `bpc_1TUf3vAhxqX4McFQ2R2Tb72j`, Webhook `we_1TUfJPAhxqX4McFQM0W5R5M7`
- Production frontend: `https://scrape.snowforge.dev` (Vercel)
- Production API: `https://2pg2gj4048.execute-api.us-east-2.amazonaws.com`

---

## What's NOT DONE

### Analytics Dashboard (UI only, no data)
- Dashboard pages exist but display mock/placeholder data
- No real analytics pipeline connected

### API Key Authentication (jobs data-plane wired on claude-main; control-plane still Clerk-only)
- Backend `api_key_handler.py` and Settings → API Keys tab fully implemented (one-time-secret modal, list/create/revoke)
- API keys can be created and managed
- DONE (claude-main, 2026-06-06): `Authorization: Bearer sk_live_...` now authenticates the entire `/jobs` data-plane via `resolve_user_id` (create, list, get, update, delete, pause, cancel, resume, refresh, crawls, download, preview)
- DONE (claude-main, 2026-06-07): public API docs + programmatic quickstart at `docs/API.md`, and `backend/openapi.yml` now documents the dual auth model (`ApiKeyAuth` scheme + `security` on the 13 data-plane ops), guarded by `test_openapi_spec.py`
- Remaining: decide whether to extend API-key auth to the other data endpoints (templates, webhooks, export destinations); the control-plane (api-keys CRUD, billing, integrations/OAuth) stays Clerk-only by design.
- Sub-project #2 of launch sequence

### Notification System (Partial)
- Webhook notifications work
- Email notifications not implemented
- In-app notification center not built

### Known Technical Limitations
- JavaScript rendering: GLIBC incompatibility blocks Playwright on Lambda (Tiers 3/4 use Firecrawl instead)
- No Redis cache (planned but not implemented)
- Single region (us-east-2, no multi-region failover)
- Lambda runs in default VPC

---

## Estimates to Revenue

| Work Item | Estimate | Priority |
|-----------|----------|----------|
| First real $0 trial signup as final live smoke | hours | CRITICAL -- final acceptance |
| API key auth on public endpoints | jobs data-plane DONE (2026-06-06); API docs + openapi spec DONE (2026-06-07); optional: extend to templates/webhooks/destinations | MEDIUM |
| Real analytics data pipeline | 2-3 weeks | HIGH |
| Email notifications | 1 week | MEDIUM |
| In-app notification center | 1 week | LOW |
| `@snowforge/ui` v3→v4 migration + revert `typescript.ignoreBuildErrors` | 1-2 hours | LOW (debt) |

---

## Architecture Reference

See [docs/INFRASTRUCTURE.md](./docs/INFRASTRUCTURE.md) for full details on:
- 29 Lambda functions with memory/timeout configs
- 8 DynamoDB tables with GSIs and encryption
- 4 SQS queues with retry and DLQ configuration
- S3 lifecycle policies
- Environment variables
- Cost estimates
- Security policies
